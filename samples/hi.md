# Markdown Viewer Ultra — फ़ीचर शोकेस

अगर आप इसे शीर्षकों, कोड के रंगों, तालिकाओं और कार्य सूचियों के साथ देख पा रहे हैं, कच्चे टेक्स्ट के बजाय, तो एक्सटेंशन ठीक से काम कर रहा है।

---

## टेक्स्ट फ़ॉर्मेटिंग

इस पैराग्राफ़ में **बोल्ड**, _इटैलिक_, ***बोल्ड इटैलिक***, ~~स्ट्राइकथ्रू~~, `इनलाइन कोड`, और एक [प्रोजेक्ट का लिंक](https://github.com/danarrib/markdown-viewer-ultra) मिले-जुले हैं।

आप इनलाइन HTML के साथ H<sub>2</sub>O और E = mc<sup>2</sup> भी लिख सकते हैं, और कीबोर्ड संकेत के लिए <kbd>Ctrl</kbd> + <kbd>C</kbd> भी।

---

## शीर्षक

# H1 — दस्तावेज़ शीर्षक
## H2 — खंड
### H3 — उप-खंड
#### H4 — समूह
##### H5 — विवरण
###### H6 — फ़ुटनोट स्तर

---

## सूचियाँ

### अनुक्रमरहित (नेस्टिंग के साथ)

- पहला आइटम
- दूसरा आइटम
  - नेस्टेड A
  - नेस्टेड B
    - गहराई से नेस्टेड
- तीसरा आइटम

### अनुक्रमित

1. टोस्टर चालू करें
2. ब्रेड डालें
3. क्लिक की आवाज़ का इंतज़ार करें
4. मक्खन लगाएँ

### कार्य सूची

- [x] कंटेंट प्रकार पहचानें
- [x] markdown रेंडर करें
- [x] HTML को सुरक्षित करें
- [x] यूज़र इंटरफ़ेस का अनुवाद करें
- [x] सिंटैक्स हाइलाइटिंग जोड़ें
- [ ] सेटिंग्स पेज जोड़ें

---

## कोड ब्लॉक

### JavaScript

```js
// मेमोइज़ेशन के साथ फ़िबोनैचि
const fib = (() => {
  const cache = new Map([[0, 0], [1, 1]]);
  return function f(n) {
    if (cache.has(n)) return cache.get(n);
    const value = f(n - 1) + f(n - 2);
    cache.set(n, value);
    return value;
  };
})();

console.log(fib(50)); // 12586269025
```

### Python

```python
from dataclasses import dataclass
from typing import Iterable

@dataclass(frozen=True)
class Point:
    x: float
    y: float

    def distance(self, other: "Point") -> float:
        return ((self.x - other.x) ** 2 + (self.y - other.y) ** 2) ** 0.5

def closest_pair(points: Iterable[Point]) -> tuple[Point, Point]:
    pts = list(points)
    return min(
        ((a, b) for i, a in enumerate(pts) for b in pts[i + 1:]),
        key=lambda pair: pair[0].distance(pair[1]),
    )
```

### Bash

```bash
#!/usr/bin/env bash
set -euo pipefail

# 1 KB से बड़ी हर .md फ़ाइल खोजें और उसकी पंक्तियाँ गिनें
find . -name '*.md' -size +1k -print0 \
  | while IFS= read -r -d '' file; do
      lines=$(wc -l < "$file")
      printf '%6d  %s\n' "$lines" "$file"
    done \
  | sort -rn \
  | head -10
```

### भाषा निर्दिष्ट किए बिना ब्लॉक

```
सादा ब्लॉक — कोई सिंटैक्स हाइलाइटिंग नहीं।
डायरेक्टरी ट्री, आरेख या कॉपी-पेस्ट नमूनों के लिए उपयोगी।
```

---

## तालिकाएँ

| विशेषता                | स्थिति | टिप्पणियाँ                            |
| ---------------------- | :----: | ------------------------------------- |
| MIME पहचान             |   ✅   | `text/markdown` और `text/x-markdown`  |
| `.md` फ़ॉलबैक          |   ✅   | जब सर्वर `text/plain` भेजे            |
| सुरक्षित करना           |   ✅   | DOMPurify                             |
| डार्क मोड              |   ✅   | स्वचालित / हल्का / गहरा               |
| स्थानीयकरण             |   ✅   | 17 भाषाएँ                             |
| सिंटैक्स हाइलाइटिंग     |   ✅   | highlight.js Common बिल्ड             |

दाईं ओर संरेखित संख्याएँ भी काम करती हैं:

| मद         | मूल्य |
| ---------- | ----: |
| कॉफ़ी      |  4.50 |
| क्रोइसैन्ट  |  3.25 |
| उप-कुल     |  7.75 |

---

## उद्धरण

> "प्रोग्राम लोगों के पढ़ने के लिए लिखे जाने चाहिए, और मशीनों के निष्पादित करने के लिए केवल आनुषंगिक रूप से।"
>
> — Harold Abelson, _Structure and Interpretation of Computer Programs_

नेस्टेड:

> बाहरी विचार।
>
> > भीतरी जवाब, **ज़ोर** और `कोड` के साथ।
> >
> > > और तीसरा स्तर, सच में पुनरावर्ती लोगों के लिए।

---

## चित्र

इनलाइन प्लेसहोल्डर:

![placeholder](https://placehold.co/600x120/png?text=Markdown+Viewer+Ultra)

---

## क्षैतिज रेखाएँ

इस पंक्ति के नीचे एक क्षैतिज रेखा है।

---

…और एक और। लंबे दस्तावेज़ों में खंडों को अलग करने के लिए उपयोगी।

---

## लिंक और संदर्भ

एक साधारण [लिंक](https://example.com), एक ऑटो-लिंक <https://example.com>, और एक [शीर्षक विशेषता](https://example.com "होवर करें") वाला लिंक।

यह रही पूरी झलक — अगर ऊपर सब कुछ अच्छी तरह दिख रहा है, तो आप पूरा फ़ीचर सेट देख रहे हैं: टाइपोग्राफ़ी, सूचियाँ, कोड हाइलाइटिंग, तालिकाएँ, उद्धरण, और थीम के अनुसार रंग।
