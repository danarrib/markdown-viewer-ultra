# Markdown Viewer Ultra — Обзор возможностей

Если вы видите это с заголовками, цветами в коде, таблицами и списками задач вместо сырого текста, расширение работает.

---

## Форматирование текста

Этот абзац содержит **жирный**, _курсив_, ***жирный курсив***, ~~зачёркнутый~~, `встроенный код` и [ссылку на проект](https://github.com/danarrib/markdown-viewer-ultra).

Можно также использовать H<sub>2</sub>O и E = mc<sup>2</sup> со встроенным HTML, и даже <kbd>Ctrl</kbd> + <kbd>C</kbd> для подсказок клавиатуры.

---

## Заголовки

# H1 — Заголовок документа
## H2 — Раздел
### H3 — Подраздел
#### H4 — Группа
##### H5 — Подробность
###### H6 — Уровень сноски

---

## Списки

### Неупорядоченный (с вложенностью)

- Первый пункт
- Второй пункт
  - Вложенный A
  - Вложенный B
    - Глубоко вложенный
- Третий пункт

### Упорядоченный

1. Включите тостер
2. Вставьте хлеб
3. Дождитесь щелчка
4. Намажьте маслом

### Список задач

- [x] Определять тип содержимого
- [x] Рендерить markdown
- [x] Очищать HTML
- [x] Локализовать интерфейс
- [x] Добавить подсветку синтаксиса
- [ ] Добавить страницу настроек

---

## Блоки кода

### JavaScript

```js
// Фибоначчи с мемоизацией
const fib = (() => {
  const cache = new Map([[0, 0], [1, 1]]);
  return function f(n) {
    if (cache.has(n)) return cache.get(n);
    const value = f(n - 1) + f(n - 2);
    cache.set(n, value);
    return value;
  };
})();

console.log(fib(50)); // 12586269025
```

### Python

```python
from dataclasses import dataclass
from typing import Iterable

@dataclass(frozen=True)
class Point:
    x: float
    y: float

    def distance(self, other: "Point") -> float:
        return ((self.x - other.x) ** 2 + (self.y - other.y) ** 2) ** 0.5

def closest_pair(points: Iterable[Point]) -> tuple[Point, Point]:
    pts = list(points)
    return min(
        ((a, b) for i, a in enumerate(pts) for b in pts[i + 1:]),
        key=lambda pair: pair[0].distance(pair[1]),
    )
```

### Bash

```bash
#!/usr/bin/env bash
set -euo pipefail

# Найти все .md-файлы размером больше 1 КБ и посчитать их строки
find . -name '*.md' -size +1k -print0 \
  | while IFS= read -r -d '' file; do
      lines=$(wc -l < "$file")
      printf '%6d  %s\n' "$lines" "$file"
    done \
  | sort -rn \
  | head -10
```

### Блок без указания языка

```
Простой блок — без подсветки синтаксиса.
Полезно для деревьев каталогов, диаграмм или примеров для копирования.
```

---

## Таблицы

| Функция                  | Статус | Примечания                            |
| ------------------------ | :----: | ------------------------------------- |
| Определение MIME         |   ✅   | `text/markdown` и `text/x-markdown`   |
| Запасной `.md`           |   ✅   | когда сервер отдаёт `text/plain`      |
| Очистка                  |   ✅   | DOMPurify                             |
| Тёмная тема              |   ✅   | авто / светлая / тёмная               |
| Локализация              |   ✅   | 17 языков                             |
| Подсветка синтаксиса     |   ✅   | highlight.js Common-сборка            |

Числа с выравниванием вправо тоже работают:

| Товар      |   Цена |
| ---------- | -----: |
| Кофе       |   4,50 |
| Круассан   |   3,25 |
| Итого      |   7,75 |

---

## Цитаты

> «Программы должны писаться для того, чтобы их читали люди, и лишь между делом для того, чтобы их исполняли машины.»
>
> — Harold Abelson, _Structure and Interpretation of Computer Programs_

Вложенная:

> Внешняя мысль.
>
> > Внутренний ответ, с **акцентом** и `кодом`.
> >
> > > И третий уровень, для по-настоящему рекурсивных.

---

## Изображения

Встроенный плейсхолдер:

![placeholder](https://placehold.co/600x120/png?text=Markdown+Viewer+Ultra)

---

## Горизонтальные линии

Под этой строкой находится горизонтальная линия.

---

…и ещё одна. Полезно для разделения секций в длинных документах.

---

## Ссылки и сноски

Простая [ссылка](https://example.com), авто-ссылка <https://example.com> и ссылка с [атрибутом title](https://example.com "Наведите курсор").

Вот и весь тур — если всё выше выглядит аккуратно, вы видите полный набор возможностей: типографика, списки, подсветка кода, таблицы, цитаты и цвета, следующие за темой.
